{
  "name": "golfapp-tests",
  "private": true,
  "description": "Regression tests for GolfApp's money-critical golf logic. Zero dependencies — uses Node's built-in test runner (node:test) so this project stays vanilla JS with no build step, matching the app itself.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
